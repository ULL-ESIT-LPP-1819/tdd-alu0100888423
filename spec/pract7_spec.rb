require "spec_helper"

RSpec.describe Pract7 do
  it "has a version number" do
    expect(Pract7::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
